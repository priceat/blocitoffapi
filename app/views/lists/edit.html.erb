<h1>Edit List</h1>
 
 <div class="row">
   <div class="col-md-4">
     <p>Suggestions for Your List</p>
     <ul>
       <li>Make sure you can get it done.</li>
       <li>Prioritize accordingly.</li>
       <li>The tough stuff should come first.</li>
     </ul>
   </div>
   <div class="col-md-8">
     <%= form_for @list do |f| %>
       <div class="form-group">
         <%= f.label :title %>
         <%= f.text_field :title, class: 'form-control'%>
       </div>
       <div class="form-group">
         <%= f.label :description %>
         <%= f.text_area :description, rows: 8, class: 'form-control' %>
       </div>
       <div class="form-group">
         <%= f.submit "Save", class: 'btn btn-success' %>
       </div>
     <% end %>
   </div>
 </div>